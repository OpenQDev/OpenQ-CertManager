apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: openq-cluster-issuer
  namespace: cert-manager
spec:
  acme:
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    email: alo42@email.com
    privateKeySecretRef:
      name: account-key-staging
    solvers:
      - http01:
          ingress:
            class: nginx
